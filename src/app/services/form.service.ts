import { Injectable } from "@angular/core";
import { FieldConfig, FormConfig } from "../models/form.model";

@Injectable({
    providedIn: 'root'
})
export class FormGenerator {
    generateFields(field: FieldConfig) {

    }

    generateForm(form: FormConfig) {
        
    }
}